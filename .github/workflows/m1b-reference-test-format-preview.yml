name: Preview M1-B reference test formatting

on:
  push:
    branches:
      - feat/m1b-ingestion-hardening

permissions:
  contents: read

jobs:
  preview:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feat/m1b-ingestion-hardening
      - uses: pnpm/action-setup@v6
        with:
          version: 11.24.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Print Prettier patches
        run: |
          for file in packages/openapi/tests/unit/reference-graph.test.ts packages/openapi/tests/integration/multi-file-reference.integration.test.ts; do
            target="/tmp/$(basename "$file")"
            pnpm exec prettier "$file" > "$target"
            diff -u "$file" "$target" || true
          done
