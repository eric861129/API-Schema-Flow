name: Publish verified M3-A implementation

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  parked:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Publisher is parked while the authenticated patch is staged."
