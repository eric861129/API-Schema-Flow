name: M3-B1 Task 6 Source Export V3

on:
  push:
    branches:
      - feat/m3b1-review-workspace-integration
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: m3b1-task6-source-export-v3
  cancel-in-progress: true

jobs:
  export:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out exact integration branch
        uses: actions/checkout@v6
        with:
          ref: feat/m3b1-review-workspace-integration
          fetch-depth: 0

      - name: Package source-only workspace
        shell: bash
        run: |
          set -euo pipefail
          tar \
            --exclude=.git \
            --exclude=node_modules \
            --exclude='**/node_modules' \
            --exclude=.turbo \
            --exclude='**/.turbo' \
            --exclude='**/dist' \
            --exclude='**/coverage' \
            --exclude='**/playwright-report' \
            --exclude='**/test-results' \
            -czf /tmp/api-schema-flow-task6-source.tgz .

      - name: Upload exact Task 6 workspace
        uses: actions/upload-artifact@v4
        with:
          name: api-schema-flow-task6-source
          path: /tmp/api-schema-flow-task6-source.tgz
          retention-days: 1
          if-no-files-found: error
